#!/usr/bin/env python

import sys
import simplejson as json

json_input = sys.stdin.read()
try:
  json_object = json.loads(json_input)
except ValueError:
  print "ERROR: Couldn't parse stdin as JSON. Input:"
  print json_input

print json.dumps(json_object, indent=2, sort_keys=True)
